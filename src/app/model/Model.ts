export class Model{
  time: string;
  distance: string;
  date: string;

  constructor( time: string, distance: string, date: string) {
    this.time = time;
    this.distance = distance;
    this.date = date;
  }
}
